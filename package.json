{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "deploy": "push-dir --dir=.output/public --branch=gh-pages --dotfiles --cleanup"
  },
  "devDependencies": {
    "@nuxt/content": "^2.0.1",
    "@nuxtjs/color-mode": "^3.1.4",
    "nuxt": "3.0.0-rc.8",
    "push-dir": "^0.4.1",
    "sass": "^1.54.4",
    "sass-loader": "^13.0.2",
    "sitemap": "^7.1.1"
  }
}
